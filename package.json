{
  "name": "toyrobot",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "directories": {
    "src": "src",
    "test": "test"
  },
  "scripts": {
    "test": "./node_modules/.bin/nyc --check-coverage --reporter=html --reporter=lcov --reporter=text ./node_modules/.bin/mocha",
    "test-lcov": "./node_modules/.bin/nyc --check-coverage --reporter=lcov ./node_modules/.bin/mocha"

  },
  "author": "nikki renvoize",
  "license": "ISC",
  "dependencies": {
    "commander": "^3.0.1"
  },
  "devDependencies": {
    "mocha": "^6.2.0",
    "nyc": "^14.1.1"
  }
}
